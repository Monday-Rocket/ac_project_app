# 코드 생성

freezed, json_serializable, flutter_gen 등 코드를 생성합니다.

## 실행 명령어

```bash
fvm dart run build_runner build --delete-conflicting-outputs
fvm dart run flutter_gen -c pubspec.yaml
```

모델 파일 변경 후 이 명령어를 실행하세요.
